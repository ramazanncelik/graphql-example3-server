import { handleURLSearchParams } from './utils.js';
export function isGETRequest(request) {
    return request.method === 'GET';
}
export function parseGETRequest(_request, url) {
    return handleURLSearchParams(url.searchParams);
}
